{
  "name": "sabores-e-saberes",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm run build && node scripts/check-components.mjs && node scripts/check-conteudo.mjs && node scripts/check-page.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  },
  "dependencies": {
    "feather-icons": "^4.29.2"
  }
}
